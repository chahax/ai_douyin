---
doc_status: current
doc_category: prompt
last_reviewed: 2026-05-10
model_usage: 当前 prompt 文件目录；这里的 txt 文件可作为当前生成流程参考。
---

> 文档状态：当前 prompt 文件目录；这里的 txt 文件可作为当前生成流程参考。

# prompts 目录说明

这里保存当前可用的 prompt 文本：

- `book-extraction.txt`
- `dialogue-generation.txt`
- `script-generation.txt`

这些 `.txt` 文件本身不加 YAML 元数据，避免影响后续直接作为 prompt 使用。
